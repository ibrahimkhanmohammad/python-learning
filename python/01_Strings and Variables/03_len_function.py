# Take input from user and store it in variable 'name'
name = input("enter name: ")

# len() function → returns total number of characters in the string
# includes spaces also
print(len(name))   # prints length of the entered name